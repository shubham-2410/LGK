## Packages
date-fns | Date formatting and manipulation for the booking calendar
embla-carousel-react | Smooth sliding banners for the home page
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
- Images should use the provided @assets/image_1772364975598.png as a fallback/placeholder.
- Authentication uses HTTP-only cookies; /api/auth/me returns 401 when not logged in.
